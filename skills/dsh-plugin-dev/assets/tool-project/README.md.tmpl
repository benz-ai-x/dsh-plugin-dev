# __PACKAGE_NAME_RAW__

__DESCRIPTION_RAW__

__PROJECT_INTRO_RAW__

## Development

__DEVELOPMENT_CONTEXT_RAW__

```sh
pnpm install
pnpm verify
```

__CONTEXT_SYNC_SECTION_RAW__

`pnpm verify` __VERIFY_CONTEXT_DESCRIPTION_RAW__,
typechecks, runs the real Tool registry and HMR tests, boots
`tests/fixtures/cordis.yml` through the Cordis Loader, builds declarations and
JavaScript, imports the built package through its public export, and inspects
a real generated `.tgz` archive.

## Profile installation

After implementing the business behavior and building the package, install the current checkout into a development profile from the project directory:

```sh
dsh plugin --profile demo add .
dsh --profile demo --dump-config
dsh --profile demo
dsh plugin --profile demo remove __PACKAGE_NAME_RAW__
```

The package ships `cordis.patch.yml` as its bundle layer. The stable row id is `__ROW_ID_RAW__`; a later profile patch that replaces this row's `config` must restate every required field.

## Delivery status

__DELIVERY_STATUS_TEXT_RAW__
